export PLANETARY_IMAGER_MAJOR_VERSION="${MAJOR_VERSION}"
export PLANETARY_IMAGER_MINOR_VERSION="${MINOR_VERSION}"
export PLANETARY_IMAGER_PATCH_VERSION="${PATCH_VERSION}"
export PLANETARY_IMAGER_VERSION_SUFFIX="${PACKAGE_VERSION_SUFFIX}"
export PLANETARY_IMAGER_VERSION="${FULL_VERSION}"
