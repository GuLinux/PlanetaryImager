export CMAKE_BUILD_TYPE=Debug
export PACKAGE_NAME_SUFFIX=_debian-${DEBIAN_VERSION}
export COMPOSE_IMAGE=gulinux/planetaryimager_build:debian-${DEBIAN_VERSION}-${TARGET_ARCH}
export PACKAGE_VERSION_SUFFIX=
