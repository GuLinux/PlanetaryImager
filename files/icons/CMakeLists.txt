file(GLOB_RECURSE icons *.png)
foreach(icon ${icons})
  get_filename_component(size ${icon} DIRECTORY)
  get_filename_component(size ${size} NAME)
  install(FILES ${icon} DESTINATION share/icons/hicolor/${size}/apps/)
endforeach(icon)
